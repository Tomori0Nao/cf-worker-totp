{
	"name": "cf-worker-topt",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"base32-decode": "^1.0.0",
		"hono": "^4.8.12"
	},
	"devDependencies": {
		"@types/node": "^24.1.0",
		"wrangler": "^4.27.0"
	}
}
